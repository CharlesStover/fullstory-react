import type InitProps from '../../types/init-props';

const TEST_INIT_PROPS: InitProps = {
  debug: true,
  devMode: true,
  host: 'test-host',
  namespace: 'test-namespace',
  orgId: 'test-org-id',
  recordCrossDomainIFrames: true,
  recordOnlyThisIFrame: true,
  script: 'test-script',
};

export default TEST_INIT_PROPS;
