import type IdentifyProps from '../../types/identify-props';

const TEST_IDENTIFY_PROPS: IdentifyProps = {
  userDisplayName: undefined,
  userEmail: undefined,
  userUid: undefined,
};

export default TEST_IDENTIFY_PROPS;
